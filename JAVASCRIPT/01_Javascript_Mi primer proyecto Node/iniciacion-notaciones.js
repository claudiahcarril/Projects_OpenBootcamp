// Notaciones en Javascript
// ;  --> Sirve para delimitar el final de una línea
const b = 4;
// pero ya se puede eliminar sin que de error:
b + 5

// . --> Se utiliza en los objetos para acceder a los atributos
//  movil.anchura 

// [] --> Se utilizan para listas o arrays (para definir el array o para acceder a algún valor dentro del array)
const ar = [1, 2, 3, 4]
console.log(ar[2])

// () --> Se utilizan para Funciones
function suma(a, b) {
    return a + b
}

// {} --> Llaves para objetos, funciones y estructuras de control
const movil = {
    anchura: 5,
    altura: 10,
}

if (true) {
    // todo lo que haya entre llaves
}

